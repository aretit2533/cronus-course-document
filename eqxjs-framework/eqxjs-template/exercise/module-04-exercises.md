# Module 4 Exercises: Kafka and Data Flow

1. Add a new Kafka topic to `topics.consume` and wire a new handler method in `EventConsumerController`.
2. Add a new producer method in `EventProducerService` with a dedicated topic name.
3. Add a repository method to query by a specific field and log the response time.
