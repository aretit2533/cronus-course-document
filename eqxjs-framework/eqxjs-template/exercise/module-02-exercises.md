# Module 2 Exercises: Setup and Configuration

1. Create a new configuration file named `dev.local.config.yaml` and update `ZONE` to use it.
2. Add a new topic name under `topics.consume` and describe how it would be used in code.
3. Document the minimum required environment variables for local startup.
